-- Generated from v0.4.181 0db48b775e76b4d62f08d78e8846d53cc427180a
PRAGMA user_version = 32;
CREATE TABLE dashboard_rows (kanji TEXT PRIMARY KEY, jiten_rank INTEGER, primary_meaning TEXT NOT NULL, reading TEXT NOT NULL, browser_search TEXT NOT NULL, weakness_score INTEGER NOT NULL, reason_code TEXT NOT NULL, reason_text TEXT NOT NULL, active_example_count INTEGER NOT NULL, suspended_example_count INTEGER NOT NULL, mature_support_count INTEGER NOT NULL, rebuilt_at INTEGER NOT NULL);
CREATE TABLE import_decisions (sync_id INTEGER NOT NULL, kanji TEXT NOT NULL, decision TEXT NOT NULL, reason_code TEXT NOT NULL, reason_text TEXT NOT NULL, jiten_rank INTEGER, rank_known INTEGER NOT NULL, rank_min INTEGER NOT NULL, rank_max INTEGER NOT NULL, min_matching_cards INTEGER NOT NULL, source_count INTEGER NOT NULL, source_types TEXT NOT NULL, rule_types TEXT NOT NULL, source_card_ids TEXT NOT NULL, source_note_ids TEXT NOT NULL, created_at INTEGER NOT NULL, PRIMARY KEY (sync_id, kanji));
CREATE TABLE import_rule_audits (sync_id INTEGER PRIMARY KEY, created_at INTEGER NOT NULL, model_name TEXT NOT NULL, enabled_sources TEXT NOT NULL, rank_min INTEGER NOT NULL, rank_max INTEGER NOT NULL, min_matching_cards INTEGER NOT NULL, import_tags TEXT NOT NULL, weak_fsrs_difficulty REAL NOT NULL, weak_lapses INTEGER NOT NULL, browser_query TEXT NOT NULL, settings_json TEXT NOT NULL);
CREATE TABLE kanji_examples (id INTEGER PRIMARY KEY AUTOINCREMENT, kanji TEXT NOT NULL, source_type TEXT NOT NULL, card_id INTEGER NOT NULL, note_id INTEGER NOT NULL, expression TEXT NOT NULL, reading TEXT NOT NULL, meaning TEXT NOT NULL, sentence TEXT NOT NULL, mature INTEGER NOT NULL, lapses INTEGER NOT NULL, interval_days INTEGER NOT NULL DEFAULT 0, reps INTEGER NOT NULL DEFAULT 0, fsrs_stability REAL, fsrs_difficulty REAL, fsrs_retrievability REAL);
CREATE TABLE kanji_inventory (kanji TEXT PRIMARY KEY, primary_meaning TEXT NOT NULL, readings TEXT NOT NULL, browser_search TEXT NOT NULL, search_text TEXT NOT NULL, source_count INTEGER NOT NULL, example_count INTEGER NOT NULL, first_seen_at INTEGER NOT NULL, last_seen_at INTEGER NOT NULL);
CREATE TABLE kanji_mnemonic_notes (kanji TEXT PRIMARY KEY, note TEXT NOT NULL, updated_at INTEGER NOT NULL);
CREATE TABLE kanji_reading_pool (kanji TEXT NOT NULL, reading TEXT NOT NULL, attested INTEGER NOT NULL DEFAULT 0, PRIMARY KEY (kanji, reading));
CREATE TABLE kanji_reading_usage (kanji TEXT NOT NULL, reading TEXT NOT NULL, expression TEXT NOT NULL, note_id INTEGER NOT NULL, source_type TEXT NOT NULL, mature INTEGER NOT NULL, lapses INTEGER NOT NULL, interval_days INTEGER NOT NULL, PRIMARY KEY (kanji, reading, note_id));
CREATE TABLE kanji_timeline_events (id INTEGER PRIMARY KEY AUTOINCREMENT, kanji TEXT NOT NULL, occurred_at INTEGER NOT NULL, event_type TEXT NOT NULL, title TEXT NOT NULL, detail TEXT NOT NULL, source_expression TEXT NOT NULL, source_reading TEXT NOT NULL, rating TEXT NOT NULL, writing_required INTEGER NOT NULL DEFAULT 0, writing_passed INTEGER NOT NULL DEFAULT 0, manual_override INTEGER NOT NULL DEFAULT 0, weakness_score INTEGER, mature_support_count INTEGER, sync_id INTEGER, dedupe_key TEXT NOT NULL);
CREATE TABLE learning_repeats (kanji TEXT NOT NULL, answer_signature TEXT NOT NULL DEFAULT '', task_type TEXT NOT NULL, repeat_type TEXT NOT NULL, step_index INTEGER NOT NULL, due_at INTEGER NOT NULL, active_token TEXT NOT NULL DEFAULT '', created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL, PRIMARY KEY (kanji, answer_signature, task_type));
CREATE TABLE local_kanji_suspensions (kanji TEXT PRIMARY KEY, suspended_at INTEGER NOT NULL);
CREATE TABLE review_log (id INTEGER PRIMARY KEY AUTOINCREMENT, kanji TEXT NOT NULL, token TEXT NOT NULL UNIQUE, rating TEXT NOT NULL, writing_required INTEGER NOT NULL, writing_passed INTEGER NOT NULL, manual_override INTEGER NOT NULL, reviewed_at INTEGER NOT NULL, review_day_start INTEGER NOT NULL DEFAULT 0, task_type TEXT NOT NULL DEFAULT '', answer_signature TEXT NOT NULL DEFAULT '', prompt TEXT NOT NULL DEFAULT '', hints_used INTEGER NOT NULL DEFAULT 0, writing_clean INTEGER NOT NULL DEFAULT 0, memory_before TEXT NOT NULL DEFAULT '', memory_after TEXT NOT NULL DEFAULT '', scheduler_state_before_json TEXT NOT NULL DEFAULT '', scheduler_state_after_json TEXT NOT NULL DEFAULT '', core_skill TEXT NOT NULL DEFAULT '', failure_cause TEXT NOT NULL DEFAULT '', evidence_source TEXT NOT NULL DEFAULT '', selected_answer TEXT NOT NULL DEFAULT '', correct_answer TEXT NOT NULL DEFAULT '', answer_evidence_json TEXT NOT NULL DEFAULT '');
CREATE TABLE settings (key TEXT PRIMARY KEY, value TEXT NOT NULL, updated_at INTEGER NOT NULL);
CREATE TABLE similar_kanji_choice_state (target_kanji TEXT NOT NULL, choice_signature TEXT NOT NULL, primary_meaning TEXT NOT NULL, choices TEXT NOT NULL, due_at INTEGER NOT NULL, passed_at INTEGER NOT NULL DEFAULT 0, last_reviewed_at INTEGER NOT NULL DEFAULT 0, correct_count INTEGER NOT NULL DEFAULT 0, wrong_count INTEGER NOT NULL DEFAULT 0, active_token TEXT NOT NULL DEFAULT '', first_seen_at INTEGER NOT NULL, last_seen_at INTEGER NOT NULL, PRIMARY KEY (target_kanji, choice_signature));
CREATE TABLE similar_kanji_pairs (kanji_a TEXT NOT NULL, kanji_b TEXT NOT NULL, source TEXT NOT NULL, first_seen_at INTEGER NOT NULL, last_seen_at INTEGER NOT NULL, PRIMARY KEY (kanji_a, kanji_b, source));
CREATE TABLE similar_kanji_repair_queue (id INTEGER PRIMARY KEY AUTOINCREMENT, target_kanji TEXT NOT NULL, repair_kanji TEXT NOT NULL, choice_signature TEXT NOT NULL, wrong_selection TEXT NOT NULL, prompt_meaning TEXT NOT NULL, status TEXT NOT NULL, due_at INTEGER NOT NULL, active_token TEXT NOT NULL DEFAULT '', attempts INTEGER NOT NULL DEFAULT 0, created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL, completed_at INTEGER NOT NULL DEFAULT 0);
CREATE TABLE similar_kanji_review_log (id INTEGER PRIMARY KEY AUTOINCREMENT, target_kanji TEXT NOT NULL, choice_signature TEXT NOT NULL, selected_kanji TEXT NOT NULL, correct INTEGER NOT NULL, reviewed_at INTEGER NOT NULL, rung TEXT NOT NULL DEFAULT 'similar_kanji');
CREATE TABLE source_cards (card_id INTEGER PRIMARY KEY, note_id INTEGER NOT NULL, deck_name TEXT NOT NULL, ord INTEGER NOT NULL, queue INTEGER NOT NULL, type INTEGER NOT NULL, due INTEGER NOT NULL, interval_days INTEGER NOT NULL, reps INTEGER NOT NULL, lapses INTEGER NOT NULL, fsrs_stability REAL, fsrs_difficulty REAL, fsrs_retrievability REAL, last_seen_sync_id INTEGER NOT NULL);
CREATE TABLE source_notes (note_id INTEGER PRIMARY KEY, model_name TEXT NOT NULL, expression TEXT NOT NULL, reading TEXT NOT NULL, meaning TEXT NOT NULL, sentence TEXT NOT NULL, fields_json TEXT NOT NULL, tags TEXT NOT NULL, last_seen_sync_id INTEGER NOT NULL);
CREATE TABLE stats_cache_state (key TEXT PRIMARY KEY, value INTEGER NOT NULL);
CREATE TABLE stats_screen_cache (id INTEGER PRIMARY KEY CHECK (id = 1), source_version INTEGER NOT NULL, generated_at INTEGER NOT NULL, cache_format_version INTEGER NOT NULL DEFAULT 1, outcome_json TEXT NOT NULL, impact_report_json TEXT NOT NULL);
CREATE TABLE study_items (kanji TEXT NOT NULL, state TEXT NOT NULL, due_at INTEGER NOT NULL, stability REAL NOT NULL, difficulty REAL NOT NULL, total_reviews INTEGER NOT NULL, lapses INTEGER NOT NULL, learning_step INTEGER NOT NULL, writing_level INTEGER NOT NULL, recognition_stage INTEGER NOT NULL DEFAULT 0, consecutive_failed_recognition_days INTEGER NOT NULL DEFAULT 0, last_failed_recognition_day INTEGER NOT NULL DEFAULT 0, writing_remediation_pending INTEGER NOT NULL DEFAULT 0, suppressed_by_task_type TEXT NOT NULL DEFAULT '', suppressed_at INTEGER NOT NULL DEFAULT 0, mature_interval_days INTEGER NOT NULL DEFAULT 0, answer_signature TEXT NOT NULL DEFAULT '', typing_meaning_memory TEXT NOT NULL DEFAULT '', meaning_kanji_memory TEXT NOT NULL DEFAULT '', kanji_meaning_memory TEXT NOT NULL DEFAULT '', font_meaning_memory TEXT NOT NULL DEFAULT '', word_reading_memory TEXT NOT NULL DEFAULT '', writing_remediation_memory TEXT NOT NULL DEFAULT '', rung TEXT NOT NULL DEFAULT 'kanji_meaning', phase TEXT NOT NULL DEFAULT 'new_learning', real_pass_streak INTEGER NOT NULL DEFAULT 0, real_again_streak INTEGER NOT NULL DEFAULT 0, last_real_review_due_at INTEGER NOT NULL DEFAULT 0, similar_kanji_memory TEXT NOT NULL DEFAULT '', kanji_reading_memory TEXT NOT NULL DEFAULT '', reading_kanji_memory TEXT NOT NULL DEFAULT '', sentence_reading_memory TEXT NOT NULL DEFAULT '', scheduler_revision INTEGER NOT NULL DEFAULT 0, routing_version INTEGER NOT NULL DEFAULT 1, adaptive_route_state_json TEXT NOT NULL DEFAULT '', active_token TEXT, created_at INTEGER NOT NULL, PRIMARY KEY (kanji, answer_signature));
CREATE TABLE study_task_log (id INTEGER PRIMARY KEY AUTOINCREMENT, task_key TEXT NOT NULL UNIQUE, kanji TEXT NOT NULL, task_type TEXT NOT NULL, started_at INTEGER NOT NULL, answered_at INTEGER NOT NULL, active_elapsed_ms INTEGER NOT NULL, outcome TEXT NOT NULL);
CREATE TABLE suspended_archive (card_id INTEGER PRIMARY KEY, note_id INTEGER NOT NULL, deck_name TEXT NOT NULL, model_name TEXT NOT NULL, expression TEXT NOT NULL, reading TEXT NOT NULL, meaning TEXT NOT NULL, sentence TEXT NOT NULL, fields_json TEXT NOT NULL, archived_at INTEGER NOT NULL, archived_sync_id INTEGER NOT NULL, restored_at INTEGER);
CREATE TABLE suspended_imports (kanji TEXT PRIMARY KEY, jiten_rank INTEGER, rank_known INTEGER NOT NULL, cutoff_used INTEGER NOT NULL, first_imported_at INTEGER NOT NULL, last_seen_sync_id INTEGER NOT NULL);
CREATE TABLE suspended_sources (kanji TEXT NOT NULL, card_id INTEGER NOT NULL, note_id INTEGER NOT NULL, expression TEXT NOT NULL, reading TEXT NOT NULL, meaning TEXT NOT NULL, sentence TEXT NOT NULL, sync_id INTEGER NOT NULL, PRIMARY KEY (kanji, card_id));
CREATE TABLE sync_card_snapshots (id INTEGER PRIMARY KEY AUTOINCREMENT, sync_id INTEGER NOT NULL, started_at INTEGER NOT NULL, finished_at INTEGER NOT NULL, card_id INTEGER NOT NULL, note_id INTEGER NOT NULL, deck_id TEXT NOT NULL DEFAULT '', deck_name TEXT NOT NULL, model_id INTEGER NOT NULL DEFAULT 0, model_name TEXT NOT NULL, ord INTEGER NOT NULL, queue INTEGER NOT NULL, type INTEGER NOT NULL, due INTEGER NOT NULL, interval_days INTEGER NOT NULL, reps INTEGER NOT NULL, lapses INTEGER NOT NULL, suspended INTEGER NOT NULL, fsrs_stability REAL, fsrs_difficulty REAL, fsrs_retrievability REAL, mature INTEGER NOT NULL);
CREATE TABLE sync_kanji_snapshots (sync_id INTEGER NOT NULL, finished_at INTEGER NOT NULL, kanji TEXT NOT NULL, active_cards INTEGER NOT NULL, suspended_cards INTEGER NOT NULL, mature_support_count INTEGER NOT NULL, average_interval_days REAL NOT NULL, total_lapses INTEGER NOT NULL, total_reps INTEGER NOT NULL, fsrs_stability_avg REAL, fsrs_difficulty_avg REAL, fsrs_retrievability_avg REAL, weakness_score INTEGER NOT NULL, reason_code TEXT NOT NULL, active_example_count INTEGER NOT NULL, suspended_example_count INTEGER NOT NULL, PRIMARY KEY (sync_id, kanji));
CREATE TABLE sync_note_snapshots (sync_id INTEGER NOT NULL, finished_at INTEGER NOT NULL, note_id INTEGER NOT NULL, model_id INTEGER NOT NULL DEFAULT 0, model_name TEXT NOT NULL, deck_ids TEXT NOT NULL DEFAULT '', deck_names TEXT NOT NULL, expression TEXT NOT NULL, reading TEXT NOT NULL, meaning TEXT NOT NULL, sentence TEXT NOT NULL, tags TEXT NOT NULL, fields_json TEXT NOT NULL, extracted_kanji TEXT NOT NULL, PRIMARY KEY (sync_id, note_id));
CREATE TABLE sync_runs (id INTEGER PRIMARY KEY AUTOINCREMENT, started_at INTEGER NOT NULL, finished_at INTEGER, status TEXT NOT NULL, active_notes_count INTEGER NOT NULL, active_cards_count INTEGER NOT NULL, suspended_cards_archived_count INTEGER NOT NULL, suspended_kanji_imported_count INTEGER NOT NULL, deleted_notes_count INTEGER NOT NULL, deleted_cards_count INTEGER NOT NULL, error_code TEXT, error_message TEXT, removal_message TEXT);
CREATE INDEX idx_dashboard_rows_priority ON dashboard_rows(weakness_score DESC, suspended_example_count DESC, kanji ASC);
CREATE INDEX idx_import_decisions_kanji_sync ON import_decisions(kanji, sync_id);
CREATE INDEX idx_kanji_examples_ordered ON kanji_examples(kanji, source_type DESC, id ASC);
CREATE INDEX idx_kanji_inventory_search ON kanji_inventory(search_text);
CREATE INDEX idx_kanji_reading_pool_reading ON kanji_reading_pool(reading);
CREATE INDEX idx_kanji_reading_usage_kanji ON kanji_reading_usage(kanji);
CREATE INDEX idx_kanji_reading_usage_reading ON kanji_reading_usage(reading);
CREATE INDEX idx_learning_repeats_due ON learning_repeats(due_at);
CREATE INDEX idx_review_log_day_reviewed ON review_log(review_day_start, reviewed_at);
CREATE INDEX idx_review_log_kanji_reviewed ON review_log(kanji, reviewed_at);
CREATE INDEX idx_review_log_rating_reviewed ON review_log(rating, reviewed_at);
CREATE INDEX idx_review_log_reviewed_at ON review_log(reviewed_at);
CREATE INDEX idx_similar_choice_due ON similar_kanji_choice_state(passed_at, due_at);
CREATE INDEX idx_similar_kanji_pairs_a ON similar_kanji_pairs(kanji_a);
CREATE INDEX idx_similar_kanji_pairs_b ON similar_kanji_pairs(kanji_b);
CREATE INDEX idx_similar_repair_due ON similar_kanji_repair_queue(status, due_at, created_at);
CREATE INDEX idx_similar_review_log_target ON similar_kanji_review_log(target_kanji, reviewed_at);
CREATE INDEX idx_study_due ON study_items(state, due_at);
CREATE INDEX idx_study_items_ladder_stats ON study_items(state, phase, rung);
CREATE INDEX idx_study_task_log_answered ON study_task_log(answered_at);
CREATE INDEX idx_sync_card_snapshots_note ON sync_card_snapshots(sync_id, note_id);
CREATE UNIQUE INDEX idx_sync_card_snapshots_sync_card ON sync_card_snapshots(sync_id, card_id);
CREATE INDEX idx_sync_kanji_snapshots_kanji_finished ON sync_kanji_snapshots(kanji, finished_at);
CREATE INDEX idx_sync_kanji_snapshots_kanji_sync ON sync_kanji_snapshots(kanji, sync_id);
CREATE INDEX idx_sync_note_snapshots_kanji ON sync_note_snapshots(sync_id, extracted_kanji);
CREATE UNIQUE INDEX idx_timeline_dedupe ON kanji_timeline_events(dedupe_key);
CREATE INDEX idx_timeline_kanji_time ON kanji_timeline_events(kanji, occurred_at, id);
INSERT INTO stats_cache_state (key, value) VALUES ('stats_source_version', 1);
